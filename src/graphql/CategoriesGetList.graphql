query CategoriesGetList($first: Int!) {
	categories(first: $first) {
		edges {
			node {
				id
				name
				slug
				backgroundImage {
					url
					alt
				}
				metadata {
					key
					value
				}
				products {
					totalCount
				}
			}
		}
	}
}
